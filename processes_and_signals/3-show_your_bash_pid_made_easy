#!/bin/bash

# Find the PIDs of processes whose names contain the word "bash"
pids=$(pgrep -f bash)

# Loop through each PID and display the PID and process name
for pid in $pids; do
    process_name=$(ps -o comm= -p $pid)
    echo "PID: $pid, Process Name: $process_name"
done
